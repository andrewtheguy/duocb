// The start-a-connection screen: hosting credentials per mode + the session
// panel once paired.

import { UiState, Actions } from "globals.slint";
import { Body } from "widgets.slint";

export component ServerScreen inherits Rectangle {
    out property <bool> field-focused: false;

    Body { text: "server (todo)"; }
}
