// The join-a-connection screen: per-mode credential forms + the session panel
// once paired.

import { UiState, Actions } from "globals.slint";
import { Body } from "widgets.slint";

export component ClientScreen inherits Rectangle {
    out property <bool> field-focused: false;

    Body { text: "client (todo)"; }
}
